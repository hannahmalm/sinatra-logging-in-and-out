#create a user class and a table to store users
#users should have a username, password, and balance (decimal)
#db folder is seeds.rb  - run rake db:migrate and rake db:seed
class User < ActiveRecord::Base 
end 
